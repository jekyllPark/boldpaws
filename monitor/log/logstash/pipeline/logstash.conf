input {
    tcp {
        port => 5000
        codec => json_lines
    }
}
output {
    elasticsearch {
            hosts => ["49.254.179.217:9200"]
            index => "logstash-%{+YYYY.MM.dd}"
    }
}